import 'package:flutter/material.dart';

class KycStepAddress extends StatelessWidget {
  const KycStepAddress({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
