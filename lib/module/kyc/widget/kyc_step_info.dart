import 'package:flutter/material.dart';

class KycStepInfo extends StatelessWidget {
  const KycStepInfo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
